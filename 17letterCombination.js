let letterCombinations = function(digits) {
    let result = []
    if (Number(digits) == 1 || digits.length == 0) return [];
    let digitsMap = {
        '2': 'abc',
        '3': 'def',
        '4': 'ghi',
        '5': 'jkl',
        '6': 'mn0',
        '7': 'pqrs',
        '8': 'tuv',
        '9': 'wxyz'
    }
    let digitsArr = Array.from()
}